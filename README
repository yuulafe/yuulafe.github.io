# Global setup #
# Download and install Git
  git config --global user.name "Changhyun lee"
  git config --global user.email leechwin@nate.com

# Make workspace or git clone
  mkdir leechwin.github.com
  cd leechwin.github.com
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:leechwin/leechwin.github.com.git
  git push origin master

# git push
  git add *     # file name
  git commit -a # input comment
  git push
